/* The focus ring for a keyboard-scrollable container (tabIndex 0, named). Inset, because these
   usually sit flush inside an overflow-hidden card that would clip an outer ring. */
export const scrollRegionClass =
  "rounded-[inherit] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-ring";
