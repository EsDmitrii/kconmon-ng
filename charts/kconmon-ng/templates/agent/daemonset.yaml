apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "kconmon-ng.agent.fullname" . }}
  labels:
    {{- include "kconmon-ng.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
spec:
  selector:
    matchLabels:
      {{- include "kconmon-ng.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent
  minReadySeconds: 5
  {{- with .Values.agent.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      {{- /* The CONFIG's fingerprint, so a values change restarts the pod that reads it.
             The agent and the controller share kconmon-ng-config and read it ONCE at startup, and a
             mounted ConfigMap changes under a running process without telling it. Helm updated the
             object and left the pods on the old file: `controller.events.enabled: true` was applied
             and the controller went on advertising no capabilities, so the Console's realtime
             ingester retried forever against a stream that was configured but not running -- with no
             error anywhere, because nothing was wrong except the generation of one file. */}}
      {{- $annotations := dict "checksum/config" (include (print $.Template.BasePath "/shared/configmap.yaml") . | sha256sum) }}
      {{- if .Values.agent.zone }}
      {{- $_ := set $annotations "kconmon-ng.zone" .Values.agent.zone }}
      {{- end }}
      {{- with .Values.agent.podAnnotations }}
      {{- range $k, $v := . }}
      {{- $_ := set $annotations $k $v }}
      {{- end }}
      {{- end }}
      annotations:
        {{- range $k, $v := $annotations }}
        {{ $k }}: {{ $v | quote }}
        {{- end }}
      labels:
        {{- include "kconmon-ng.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent
    spec:
      serviceAccountName: {{ include "kconmon-ng.serviceAccountName" . }}
      # The agent never calls the apiserver; the controller is the only API client.
      automountServiceAccountToken: false
      {{- with .Values.agent.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.agent.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agent.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agent.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- /* The sysctl is appended to podSecurityContext rather than living inside it, so switching it
             off does not take runAsNonRoot / runAsUser / seccompProfile with it -- the three keys a
             namespace enforcing restricted PSS requires. */}}
      {{- $podSec := deepCopy (.Values.agent.podSecurityContext | default dict) }}
      {{- if .Values.agent.pingGroupRange }}
      {{- $podSec = merge $podSec (dict "sysctls" (list (dict "name" "net.ipv4.ping_group_range" "value" "0 2147483647"))) }}
      {{- end }}
      {{- with $podSec }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: agent
          image: "{{ .Values.agent.image.repository }}:{{ .Values.agent.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.agent.image.pullPolicy }}
          securityContext:
            {{- with .Values.agent.securityContext }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.config.httpPort }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.config.metricsPort }}
              protocol: TCP
            - name: grpc
              containerPort: {{ .Values.config.grpcPort }}
              protocol: TCP
          env:
            - name: KCONMON_NG_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: KCONMON_NG_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: KCONMON_NG_POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            {{- if .Values.agent.zone }}
            - name: KCONMON_NG_ZONE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['kconmon-ng.zone']
            {{- end }}
            - name: KCONMON_NG_CONTROLLER_ADDRESS
              value: {{ include "kconmon-ng.controllerService" . | quote }}
            - name: KCONMON_NG_CONFIG
              value: /etc/kconmon-ng/config.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/kconmon-ng
              readOnly: true
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 3
            failureThreshold: 20
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.agent.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "kconmon-ng.fullname" . }}-config
